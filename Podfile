# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ParstagramiOS' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  
  pod 'AlamofireImage'
  pod 'Parse'
  pod 'MessageInputBar'

  # Pods for ParstagramiOS
  target 'ParstagramiOSTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ParstagramiOSUITests' do
    # Pods for testing
  end

end
